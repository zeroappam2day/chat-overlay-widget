---
phase: 34
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PMChatTab.tsx
  - src/store/pmChatStore.ts
  - src/components/AgentSidebar.tsx
  - src/components/TerminalPane.tsx
  - sidecar/src/pmChat.ts
  - "# plus any dead code files flagged by repowise"
autonomous: false
requirements:
  - CLEAN-01
  - CLEAN-02

must_haves:
  truths:
    - "App starts with zero console errors from missing imports or unresolved module references"
    - "Switching to the PM Chat tab renders PMChatTab without errors — health check fires when wsSend is available"
    - "Every file flagged by repowise get_dead_code() is deleted or has an inline retention comment"
  artifacts:
    - path: "src/components/PMChatTab.tsx"
      provides: "PM Chat UI — renders in AgentSidebar when activeTab === pm-chat"
    - path: "src/store/pmChatStore.ts"
      provides: "Zustand store with wsSend, messages, streaming, health state"
    - path: "src/components/TerminalPane.tsx"
      provides: "Calls setWsSend on WS connect/disconnect"
    - path: "sidecar/src/pmChat.ts"
      provides: "streamOllamaChat, cancelOllamaChat, checkOllamaHealth — imported by server.ts line 54"
  key_links:
    - from: "src/components/TerminalPane.tsx"
      to: "src/store/pmChatStore.ts"
      via: "usePmChatStore.getState().setWsSend(sendMessage) on WS open"
    - from: "src/components/PMChatTab.tsx"
      to: "src/store/pmChatStore.ts"
      via: "usePmChatStore selector — reads wsSend, messages, streaming, health"
    - from: "sidecar/src/server.ts"
      to: "sidecar/src/pmChat.ts"
      via: "import at line 54 — case pm-chat, pm-chat-cancel, pm-chat-health-check"
---

<objective>
Verify PMChatTab/pmChatStore/pmChat are properly wired end-to-end, fix any broken flows, then run repowise dead code scan and delete confirmed-unused files.

Purpose: Phase 35 and 36 build directly on these modules. A half-wired state now causes cascading failures later.
Output: Clean codebase — TypeScript compiles, app starts without console errors, no confirmed dead code remains.
</objective>

<execution_context>
@C:/Users/anujd/Documents/01_AI/214_Chat_overlay_widget/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/anujd/Documents/01_AI/214_Chat_overlay_widget/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

## What Is Already Wired (verified at plan time)

- `AgentSidebar.tsx` imports `PMChatTab` and renders `<PMChatTab />` when `activeTab === 'pm-chat'` (line 189)
- `agentEventStore.ts` has `activeTab: 'agent' | 'pm-chat'` and `setActiveTab` sets `collapsed: false`
- `pmChatStore.ts` has complete Zustand store: `wsSend`, `messages`, `streaming`, `health`, `setWsSend`
- `TerminalPane.tsx` calls `usePmChatStore.getState().setWsSend(sendMessage)` on WS open (line 232) and `setWsSend(null)` on close (line 236)
- `PMChatTab.tsx` guards all `wsSend` calls with `if (wsSend)` or optional chaining
- `server.ts` imports from `./pmChat.js` at line 54 and handles `pm-chat`, `pm-chat-cancel`, `pm-chat-health-check` WS message cases
- TypeScript (`tsc --noEmit`) compiles clean for both frontend and sidecar at plan time

## What May Still Be Broken

- Race condition: TerminalPane renders and connects WS; AgentSidebar also renders. If sidebar renders before WS connects, `wsSend` is null and health check useEffect skips. This is benign (PMChatTab re-fires on `wsSend` change via dependency array) — verify the useEffect dependency is correct.
- Any files repowise flags as dead that are actually imported — need human review before deletion.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Audit and fix PMChat wiring gaps</name>
  <files>
    src/components/PMChatTab.tsx
    src/store/pmChatStore.ts
    src/components/TerminalPane.tsx
    src/components/AgentSidebar.tsx
    sidecar/src/pmChat.ts
  </files>
  <action>
Perform a systematic wiring audit. Do not refactor — only fix broken connections.

1. Read `src/components/PMChatTab.tsx` fully. Verify:
   - The health-check useEffect has `[wsSend]` in its dependency array. If missing, add it.
   - All four store selectors (`messages`, `streaming`, `health`, `healthError`, `wsSend`) match the interface in `pmChatStore.ts`. Fix any mismatch.
   - The `pm-chat` WS message sent on submit includes `requestId`, `message`, `model`, `temperature`, `systemPrompt` fields — matching the server.ts case handler at line 1709. If any field is missing, add it with a safe default (`model: 'llama3'`, `temperature: 0`, `systemPrompt: ''`).

2. Read `src/store/pmChatStore.ts`. Verify:
   - `appendToken` finds the assistant message by `requestId`. If the last message is a different requestId, it pushes a new one. This is correct — do NOT change it.
   - No import errors — the file uses only `zustand` (no external deps beyond that).

3. Read `src/components/TerminalPane.tsx` around lines 225-240. Verify:
   - `setWsSend` is called inside the WS `open` handler (not outside it).
   - `setWsSend(null)` is called in the WS `close` handler.
   - If either call is missing or misplaced, fix it.

4. Read `sidecar/src/pmChat.ts`. Verify:
   - The exported function signatures match what server.ts imports: `streamOllamaChat`, `cancelOllamaChat`, `checkOllamaHealth`.
   - No `import` paths that reference deleted v1.7 files.

5. After any fixes, run `npx tsc --noEmit` (frontend) and `cd sidecar && npx tsc --noEmit` (sidecar). Both must exit 0 before proceeding.
  </action>
  <verify>
    <automated>cd /c/Users/anujd/Documents/01_AI/214_Chat_overlay_widget && npx tsc --noEmit && cd sidecar && npx tsc --noEmit && echo "ALL CLEAN"</automated>
  </verify>
  <done>Both TypeScript compilers exit 0. Any wiring gaps found are fixed. Output: "ALL CLEAN"</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Dead code scan and cleanup</name>
  <files>
    "# Files determined by repowise get_dead_code() output"
  </files>
  <action>
1. Call the repowise MCP tool: `get_dead_code()` — no parameters needed.

2. Review each flagged file or symbol:
   - If the file is genuinely unused (no imports found, not an entry point, not a test file): DELETE the file.
   - If the file is a test file (`*.test.ts`) for a deleted module: DELETE it too.
   - If the file is flagged but you find an import chain leading to it: add an inline comment at the top of the file: `// Retained: used by [importer] — repowise flagged false positive [date]`
   - If the file is a v1.7 module that was started but abandoned AND is not referenced anywhere: DELETE it.

3. Priority targets based on v1.7 abandon history (check these first even if repowise doesn't flag them):
   - Any file under `sidecar/src/` or `src/` with "tts", "voice", "sapi", "speech" in the name — these are v1.7 TTS leftovers.
   - Any file under `src/` with "settings" in the name that predates Phase 35 work.

4. After deletions, run `npx tsc --noEmit` again to confirm no broken imports from the deletions.

5. If a deletion breaks a TypeScript compile, either:
   a. Restore the file if it was genuinely needed, OR
   b. Remove the import that referenced it (if the import is itself dead code).

Do NOT delete test files for modules that are actively used (e.g., `pmChat.test.ts` stays — pmChat.ts is wired).
  </action>
  <verify>
    <automated>cd /c/Users/anujd/Documents/01_AI/214_Chat_overlay_widget && npx tsc --noEmit && cd sidecar && npx tsc --noEmit && echo "CLEAN AFTER DELETIONS"</automated>
  </verify>
  <done>All repowise-flagged dead code is either deleted or has a retention comment. TypeScript compiles clean after deletions. Confirmed: `pmChat.ts`, `pmChatStore.ts`, `PMChatTab.tsx` are NOT deleted (they are wired and Phase 35-36 depend on them).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - PMChat wiring audited and any gaps fixed (Task 1)
    - Dead code removed per repowise scan (Task 2)
    - TypeScript compiles clean (both frontend and sidecar)
  </what-built>
  <how-to-verify>
    1. Run `start.bat` to launch the app.
    2. Open DevTools (F12 or right-click → Inspect in the webview).
    3. Check the Console tab — confirm ZERO errors related to missing imports, unresolved modules, or undefined references from PMChat or v1.7 modules.
    4. Click the sidebar PM Chat tab icon — the PMChatTab should render (showing "Checking Ollama..." or an error state if Ollama is not running — either is correct, no blank screen or JS error).
    5. Run `stop.bat` to close.

    Expected: Console is clean on startup. PM Chat tab renders without errors (health state shows status, not a crash).
  </how-to-verify>
  <resume-signal>Type "approved" if console is clean and PM Chat tab renders, or describe any errors seen</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS message → pmChat handler | Untrusted message structure arrives from webview; server.ts casts with `as` — no runtime validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | server.ts pm-chat case | accept | Local app, single user, no auth bypass risk from cast |
| T-34-02 | Denial of Service | pmChat streaming abort | accept | cancelOllamaChat already exists; single user, no DoS surface |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (frontend)
- `cd sidecar && npx tsc --noEmit` exits 0 (sidecar)
- No v1.7 abandoned files remain without a retention comment
- App starts without console errors from module resolution or PMChat
- PM Chat tab renders (health check state — ok or error — is acceptable; crash is not)
</verification>

<success_criteria>
1. App starts without console errors caused by missing imports or unresolved v1.7 module references (CLEAN-01)
2. PMChatTab renders in the sidebar without crashing — wsSend is injected from TerminalPane, health check fires when connection is ready (CLEAN-01)
3. Every file flagged by repowise get_dead_code() is deleted or has an inline retention comment (CLEAN-02)
4. TypeScript compiles clean (both frontend and sidecar) after all changes
</success_criteria>

<output>
After completion, create `.planning/quick/260409-ly6-phase-34-orphan-and-dead-code-cleanup/260409-ly6-SUMMARY.md`
</output>
