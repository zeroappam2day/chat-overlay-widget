---
phase: 20-metadata-injection-integration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/utils/formatCaptureBlock.ts
  - src/utils/formatCaptureBlock.test.ts
autonomous: true
requirements:
  - CAPT-02
  - CAPT-03

must_haves:
  truths:
    - "formatCaptureBlock produces a multi-line string with shell-quoted path on line 1"
    - "Metadata block contains bounds x,y,w,h in physical pixel values"
    - "Metadata block contains captureSize, dpiScale, and coordinate_origin fields"
    - "Block format uses # comment lines readable by Claude without JSON parsing"
  artifacts:
    - path: "src/utils/formatCaptureBlock.ts"
      provides: "Capture metadata text formatter"
      exports: ["formatCaptureBlock", "CaptureBlockInput"]
    - path: "src/utils/formatCaptureBlock.test.ts"
      provides: "Unit tests for formatCaptureBlock"
      contains: "formatCaptureBlock"
  key_links:
    - from: "src/utils/formatCaptureBlock.ts"
      to: "src/utils/shellQuote.ts"
      via: "import quotePathForShell"
      pattern: "quotePathForShell"
---

<objective>
Create and TDD a `formatCaptureBlock()` utility that converts capture metadata into a structured text block for Claude spatial reasoning.

Purpose: CAPT-02 and CAPT-03 require a metadata block formatted alongside the image path. This utility is the single source of formatting logic, tested in isolation before wiring into components.
Output: `src/utils/formatCaptureBlock.ts` + `src/utils/formatCaptureBlock.test.ts`
</objective>

<execution_context>
@C:/Users/anujd/Documents/01_AI/214_Chat_overlay_widget/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/anujd/Documents/01_AI/214_Chat_overlay_widget/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-metadata-injection-integration/20-RESEARCH.md

@src/utils/shellQuote.ts
@src/protocol.ts
</context>

<interfaces>
<!-- Key types the executor needs -->

From src/protocol.ts (line 48):
```typescript
| { type: 'capture-result-with-metadata'; path: string; title: string; bounds: { x: number; y: number; w: number; h: number }; captureSize: { w: number; h: number }; dpiScale: number };
```

From src/utils/shellQuote.ts:
```typescript
export function quotePathForShell(filePath: string, shell: string | null | undefined): string;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD formatCaptureBlock utility</name>
  <files>src/utils/formatCaptureBlock.ts, src/utils/formatCaptureBlock.test.ts</files>
  <read_first>
    - src/utils/shellQuote.ts (import quotePathForShell — see how it handles PS/cmd/bash)
    - src/protocol.ts (capture-result-with-metadata type — fields: path, title, bounds, captureSize, dpiScale)
    - vitest.config.ts (test runner config)
  </read_first>
  <behavior>
    - Test 1: PowerShell shell — path is single-quoted via quotePathForShell, block contains `# window: Chrome`, `# bounds: x=100 y=200 w=1280 h=720 (physical pixels)`, `# capture_size: 1280x720`, `# dpi_scale: 1.2500`, `# coordinate_origin: top-left, units: physical pixels`
    - Test 2: cmd.exe shell — path is double-quoted
    - Test 3: bash.exe shell — path uses forward-slash /c/ prefix with single quotes
    - Test 4: null shell — path is unquoted raw
    - Test 5: dpiScale 1.0 formats as `1.0000` (toFixed(4))
    - Test 6: Title with special characters passes through unescaped (only path gets shell-quoted)
    - Test 7: Block has exactly 6 lines (path + 5 comment lines)
  </behavior>
  <action>
    **RED phase:** Create `src/utils/formatCaptureBlock.test.ts` with the 7 test cases above. The test file imports `formatCaptureBlock` and `CaptureBlockInput` from `./formatCaptureBlock`. Run `npx vitest run src/utils/formatCaptureBlock.test.ts` — must fail (module not found).

    **GREEN phase:** Create `src/utils/formatCaptureBlock.ts` with:

    ```typescript
    import { quotePathForShell } from './shellQuote';

    export interface CaptureBlockInput {
      path: string;
      title: string;
      bounds: { x: number; y: number; w: number; h: number };
      captureSize: { w: number; h: number };
      dpiScale: number;
      shell: string | null;
    }

    export function formatCaptureBlock(input: CaptureBlockInput): string {
      const quotedPath = quotePathForShell(input.path, input.shell);
      return [
        quotedPath,
        `# window: ${input.title}`,
        `# bounds: x=${input.bounds.x} y=${input.bounds.y} w=${input.bounds.w} h=${input.bounds.h} (physical pixels)`,
        `# capture_size: ${input.captureSize.w}x${input.captureSize.h}`,
        `# dpi_scale: ${input.dpiScale.toFixed(4)}`,
        `# coordinate_origin: top-left, units: physical pixels`,
      ].join('\n');
    }
    ```

    Run tests again — all 7 must pass.

    **REFACTOR:** No refactoring expected — implementation is minimal.
  </action>
  <verify>
    <automated>npx vitest run src/utils/formatCaptureBlock.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/utils/formatCaptureBlock.ts exports `formatCaptureBlock` and `CaptureBlockInput`
    - src/utils/formatCaptureBlock.test.ts contains at least 7 `it(` or `test(` blocks
    - `npx vitest run src/utils/formatCaptureBlock.test.ts` exits 0
    - formatCaptureBlock output line 1 contains shell-quoted path (verified by test assertions checking single-quotes for PS, double-quotes for cmd)
    - formatCaptureBlock output contains `# bounds: x=` pattern
    - formatCaptureBlock output contains `# coordinate_origin: top-left`
    - formatCaptureBlock output contains `# dpi_scale:` with 4 decimal places
  </acceptance_criteria>
  <done>formatCaptureBlock utility fully tested with 7 passing tests covering all shell types, coordinate format, and line count</done>
</task>

</tasks>

<verification>
npx vitest run src/utils/formatCaptureBlock.test.ts
</verification>

<success_criteria>
- formatCaptureBlock.ts exists and exports CaptureBlockInput interface + formatCaptureBlock function
- All 7 tests pass covering PS/cmd/bash/null shells, dpi formatting, title passthrough, line count
- Block format matches CAPT-03 computer_use coordinate convention (physical pixels, top-left origin)
</success_criteria>

<output>
After completion, create `.planning/phases/20-metadata-injection-integration/20-01-SUMMARY.md`
</output>
