---
phase: 19-window-picker-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - src/components/WindowPicker.tsx
  - src/components/__tests__/WindowPicker.test.tsx
  - package.json
autonomous: true
requirements: [PICK-01, PICK-02, PICK-03, THUMB-04]

must_haves:
  truths:
    - "WindowPicker renders a thumbnail card for each window in the windows prop"
    - "Arrow keys move selectedIndex through the grid (left/right +/-1, up/down +/-cols)"
    - "Escape calls onClose and focuses .chat-input-textarea"
    - "Search input filters windows by case-insensitive title OR processName substring"
    - "Empty filter result shows 'No matching windows' text"
    - "Refresh button calls onRefresh without calling onClose"
    - "Error thumbnails display placeholder card with error text, not hidden"
  artifacts:
    - path: "src/components/WindowPicker.tsx"
      provides: "Window picker popover component"
      exports: ["WindowPicker"]
    - path: "src/components/__tests__/WindowPicker.test.tsx"
      provides: "Unit tests for PICK-01, PICK-02, PICK-03, THUMB-04"
      min_lines: 80
  key_links:
    - from: "src/components/WindowPicker.tsx"
      to: "src/protocol.ts"
      via: "import WindowThumbnail"
      pattern: "import.*WindowThumbnail.*from.*protocol"
---

<objective>
Create the WindowPicker component with full test coverage for all Phase 19 requirements.

Purpose: Self-contained component that renders a thumbnail grid with search, keyboard navigation, and refresh — ready for integration into TerminalPane in Plan 02.
Output: WindowPicker.tsx component + passing unit tests covering PICK-01, PICK-02, PICK-03, THUMB-04.
</objective>

<execution_context>
@C:/Users/anujd/Documents/01_AI/214_Chat_overlay_widget/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/anujd/Documents/01_AI/214_Chat_overlay_widget/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-window-picker-ui/19-CONTEXT.md
@.planning/phases/19-window-picker-ui/19-RESEARCH.md

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From src/protocol.ts:
```typescript
export interface WindowThumbnail {
  title: string;
  processName: string;
  /** Base64-encoded PNG thumbnail (240x180). Absent if capture failed. */
  thumbnail?: string;
  /** Error description if thumbnail capture failed. */
  error?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install test dependencies and update vitest config</name>
  <files>package.json, vitest.config.ts</files>
  <read_first>
    - C:/Users/anujd/Documents/01_AI/214_Chat_overlay_widget/vitest.config.ts
    - C:/Users/anujd/Documents/01_AI/214_Chat_overlay_widget/package.json
  </read_first>
  <action>
1. Run: `npm install --save-dev @testing-library/react @testing-library/user-event @testing-library/jest-dom jsdom`

2. Update `vitest.config.ts` to:
   - Add `environment: 'jsdom'` to the `test` object
   - Extend `include` array to also match `*.test.tsx` files: `['src/**/*.test.ts', 'src/**/*.test.tsx', 'sidecar/src/**/*.test.ts']`

3. Create directory `src/components/__tests__/` (mkdir -p).

4. Run `npx vitest run` to confirm existing tests still pass with the new config.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts contains `environment: 'jsdom'`
    - vitest.config.ts include array contains `src/**/*.test.tsx`
    - package.json devDependencies contains `@testing-library/react`
    - package.json devDependencies contains `@testing-library/user-event`
    - package.json devDependencies contains `jsdom`
    - `npx vitest run` exits 0 (no regressions)
  </acceptance_criteria>
  <done>Test infrastructure ready: jsdom environment, @testing-library installed, existing tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create WindowPicker component with TDD</name>
  <files>src/components/__tests__/WindowPicker.test.tsx, src/components/WindowPicker.tsx</files>
  <read_first>
    - C:/Users/anujd/Documents/01_AI/214_Chat_overlay_widget/src/protocol.ts
    - C:/Users/anujd/Documents/01_AI/214_Chat_overlay_widget/src/components/SearchOverlay.tsx
  </read_first>
  <behavior>
    PICK-01 tests:
    - Renders one card per window when `windows` has 3 items (check 3 elements with role or data-testid)
    - Each card shows window title text and processName text
    - Card with `thumbnail` field renders an `<img>` with `src` starting with `data:image/png;base64,`
    - Card with `error` field (no thumbnail) renders error text in a placeholder div

    PICK-02 tests:
    - ArrowRight moves selectedIndex from 0 to 1 (second card gets `border-[#007acc]` class)
    - ArrowLeft at index 0 stays at 0 (does not go negative)
    - ArrowDown moves selectedIndex by cols (3) — from index 0 to index 3
    - ArrowUp from index 3 moves back to index 0
    - Escape calls onClose mock
    - All arrow key events call stopPropagation (verify via spy)

    PICK-03 tests:
    - Clicking Refresh button calls onRefresh mock
    - Clicking Refresh button does NOT call onClose mock

    THUMB-04 tests:
    - Typing "chrome" in search input filters to only windows whose title or processName contains "chrome" (case-insensitive)
    - Typing a string matching no windows shows "No matching windows" text
    - Clearing search shows all windows again
    - Filter matches on processName (e.g., search "explorer" matches window with processName "explorer.exe")
  </behavior>
  <action>
**RED phase — write tests first in `src/components/__tests__/WindowPicker.test.tsx`:**

```tsx
import { render, screen, fireEvent } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { describe, it, expect, vi } from 'vitest';
import { WindowPicker } from '../WindowPicker';
import type { WindowThumbnail } from '../../protocol';
```

Create test fixtures:
```typescript
const mockWindows: WindowThumbnail[] = [
  { title: 'Google Chrome', processName: 'chrome.exe', thumbnail: 'iVBORw0KGgo=' },
  { title: 'VS Code', processName: 'code.exe', thumbnail: 'iVBORw0KGgo=' },
  { title: 'File Explorer', processName: 'explorer.exe', thumbnail: 'iVBORw0KGgo=' },
  { title: 'Broken Window', processName: 'broken.exe', error: 'MINIMIZED' },
];
```

Write all test cases from the behavior block above. Use `fireEvent.keyDown` on the picker container for arrow key tests. Use `screen.getByPlaceholderText('Filter by title or process...')` for the search input. Use `screen.getByText` for title/processName assertions.

For stopPropagation test: Create a wrapper div with an onKeyDown spy, render WindowPicker inside it, fire arrow key on picker — verify wrapper spy was NOT called.

Run tests — they must all FAIL (component doesn't exist yet).

**GREEN phase — create `src/components/WindowPicker.tsx`:**

Component props interface:
```typescript
interface WindowPickerProps {
  windows: WindowThumbnail[];
  onClose: () => void;
  onRefresh: () => void;
  onSelect?: (window: WindowThumbnail) => void; // Phase 20 hook — noop for now
}
```

Implementation per decisions D-02 through D-17:
- Root div: `absolute inset-0 z-10 bg-[#1e1e1e]/95 flex flex-col` with `onKeyDown={handleKeyDown}` and `tabIndex={-1}` (per D-02)
- Header bar: flex row with search `<input>` (ref for auto-focus, placeholder "Filter by title or process..."), Refresh button (text "Refresh"), close button (text content X character)
- Search input: `ref={searchRef}`, auto-focus via `useEffect(() => { searchRef.current?.focus(); }, [])` (per D-10)
- Grid: `display: grid; gridTemplateColumns: repeat(auto-fill, minmax(260px, 1fr)); gap: 8px` (per D-06)
- Each card: `data-testid="picker-card"` for test queries. Border `border-[#007acc]` when `i === selectedIndex`, else `border-[#404040]` (per D-09)
- Thumbnail: `<img>` with `src={data:image/png;base64,${w.thumbnail}}` OR placeholder div with error text (per D-07, D-08)
- Title: `text-gray-300 text-xs truncate` (per D-07)
- Process name: `text-gray-500 text-xs truncate` (per D-07)
- Empty state: "No matching windows" centered text (per D-16)
- Filter: `useMemo` with case-insensitive substring match on title OR processName (per D-13, D-14)
- selectedIndex reset to 0 when search changes: `useEffect(() => { setSelectedIndex(0); }, [search])` (Pitfall 2)
- Keyboard handler: `e.stopPropagation()` on ALL keydowns (per D-12). ArrowRight/Left +/-1 clamped. ArrowDown/Up +/-cols (cols=3). Escape calls `onClose()` then `document.querySelector('.chat-input-textarea')?.focus()`. Enter calls `onSelect?.(filtered[selectedIndex])` (noop if no onSelect).
- Refresh button onClick: calls `onRefresh` only (per D-17)

Run tests — they must all PASS.

Commit with message: `feat(19-01): add WindowPicker component with tests`
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/WindowPicker.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/WindowPicker.tsx` exists and exports `WindowPicker`
    - File `src/components/__tests__/WindowPicker.test.tsx` exists with at least 12 test cases
    - `npx vitest run src/components/__tests__/WindowPicker.test.tsx` exits 0
    - WindowPicker.tsx contains `import type { WindowThumbnail } from '../protocol'`
    - WindowPicker.tsx contains `e.stopPropagation()` in handleKeyDown
    - WindowPicker.tsx contains `repeat(auto-fill, minmax(260px, 1fr))`
    - WindowPicker.tsx contains `border-[#007acc]` (selected card highlight)
    - WindowPicker.tsx contains `No matching windows` text for empty filter state
    - WindowPicker.tsx contains `data:image/png;base64,` for thumbnail rendering
    - WindowPicker.tsx contains `useEffect` with `searchRef.current?.focus()` (auto-focus)
  </acceptance_criteria>
  <done>WindowPicker component passes all unit tests for PICK-01 (grid rendering), PICK-02 (keyboard nav), PICK-03 (refresh), THUMB-04 (search filter).</done>
</task>

</tasks>

<verification>
1. `npx vitest run --reporter=verbose` — all tests pass (existing + new)
2. `grep -c 'test\|it(' src/components/__tests__/WindowPicker.test.tsx` — at least 12 test cases
3. `grep 'WindowThumbnail' src/components/WindowPicker.tsx` — imports protocol type
</verification>

<success_criteria>
- WindowPicker.tsx component exists with all UI elements per D-02 through D-17
- All unit tests pass covering PICK-01, PICK-02, PICK-03, THUMB-04
- No regressions in existing test suite
- Component is ready for integration into TerminalPane (Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/19-window-picker-ui/19-01-SUMMARY.md`
</output>
